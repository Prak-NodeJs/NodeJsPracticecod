// var products = [
//     {name:'cucmbar'
// , type:'vegetable'},
// {name:'banana'
// , type:'vegetable'}
// ]


// products.filter(function (product){
//      return product.type === "vegetable";
// })





// var products = [
//     {name:'cucmbar', type:'vegetable', quantity:2, price:40},
//  {name:'lauki', type:'vegetable', quantity:3, price:50},
// ]




// products.filter(function (product){
  
  
//   return product.type === 'vegetable' && product.quantity>1 && product.price>20;
  
// })




// // This is a hard one!  Create a function called 'reject'.  Reject should work in the opposite way 
// // of 'filter' - if a function returns 'true', the item should *not* be included in the new array.  Hint: you can reuse filter.

// // function reject(array, iteratorFunction) {
// //     return array.filter(function (number){
// //         return !iteratorFunction(number);
// //     })

// // }




// // Filter the array of users, only returning users
// //  who have admin level access.  Assign the result to the variable 'filteredUsers'. Don't forget to use the 'return' keyword in the function!

// var users = [
//     { id: 1, admin: true },  
//     { id: 2, admin: false },
//     { id: 3, admin: false },
//     { id: 4, admin: false },
//     { id: 5, admin: true },
//    ];
   
//    var filteredUsers = users.filter(function (user){
//         return user.admin==true;
//    })


// //    Filter the array of numbers using the 
// //    filter helper, creating a new array that only contains numbers greater than 50.  Assign this new array to a variable called 'filteredNumbers'.  Don't forget to use the 'return' keyword in the function!

// // var numbers = [15, 25, 35, 45, 55, 65, 75, 85, 95];

// // var filteredNumbers=numbers.filter(function (number){
    
// //     return (number>50);
// // });
